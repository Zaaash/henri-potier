import cartReducer, { CartState, addToCart } from './cartSlice'

describe('cart reducer', () => {
  // const initialState: CartState = {
  //   value: 3,
  //   items: [],
  //   total: 0,
  //   status: 'idle',
  // }
  // it('should handle initial state', () => {
  //   expect(cartReducer(undefined, { type: 'unknown' })).toEqual({
  //     value: 0,
  //     status: 'idle',
  //   })
  // })
  // it('should handle increment', () => {
  //   const actual = cartReducer(initialState, increment())
  //   expect(actual.value).toEqual(4)
  // })
  // it('should handle decrement', () => {
  //   const actual = cartReducer(initialState, decrement())
  //   expect(actual.value).toEqual(2)
  // })
  // it('should handle incrementByAmount', () => {
  //   const actual = cartReducer(initialState, incrementByAmount(2))
  //   expect(actual.value).toEqual(5)
  // })
})
